# Change Log

- Hardened installer secret handling by enforcing `chmod 600 .env` after environment file creation/check.
- Ensures secrets remain unreadable to non-owner local users in default root umask deployments.
