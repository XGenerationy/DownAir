# Handoff

## What changed
- Updated `install.sh` to always apply strict file permissions (`600`) to `.env`.

## Why
- Prevents local secret disclosure of JWT, DB, and token cryptographic secrets during installer-based production setup.

## Operator notes
- Existing deployments should run `chmod 600 /opt/downair/.env` once.
